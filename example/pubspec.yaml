name: example
description: A new Flutter project.
version: 1.0.0
publish_to: none

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  
  earth_kit:
    path: ../
  shared:
    path: ../../../shared/shared

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
